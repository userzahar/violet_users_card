import { LoadMoreButtonStyled } from "./LoadMoreButton.styled"

export const LoadMoreButton = ({heandleButton}) => {
    return <LoadMoreButtonStyled onClick={heandleButton}>Load More</LoadMoreButtonStyled>
}
